# نظام تحويل العملات والتتبع المتقدم لتطبيق التوصيل

تلبيةً لمتطلباتك، سأوضح هنا كيفية عمل نظام تحويل العملات الديناميكي وآلية التتبع المتقدمة التي تشمل رسم مسار الرحلة للسائق والعميل.

## 1. نظام تحويل العملات الديناميكي (Dynamic Currency Conversion)

يهدف هذا النظام إلى توفير مرونة في عرض الأسعار والمدفوعات بين الليرة السورية (SYP) والدولار الأمريكي (USD)، مع إمكانية تحكم المدير في سعر الصرف.

### أ. هيكلية البيانات (MongoDB `CurrencyRates` Collection)
كما ذكر في هيكلية البيانات، سيتم تخزين سعر الصرف في مجموعة `CurrencyRates`:

| الحقل | النوع | الوصف | ملاحظات |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | المعرف الفريد لسعر الصرف | |
| `baseCurrency` | String | العملة الأساسية | `USD` (العملة التي يتم التحويل منها) |
| `targetCurrency` | String | العملة المستهدفة | `SYP` (العملة التي يتم التحويل إليها) |
| `rate` | Number | سعر الصرف (كم ليرة سورية مقابل 1 دولار) | مطلوب، يحدده المدير |
| `updatedBy` | ObjectId | معرف المدير الذي قام بالتحديث | مرجع إلى `Users` Collection |
| `lastUpdated` | Date | تاريخ آخر تحديث لسعر الصرف | |

### ب. آلية العمل

1.  **تحديد العملة الأساسية للمطعم:** كل مطعم سيحدد العملة الأساسية لأسعاره (SYP أو USD) في إعداداته أو في حقل `currency` ضمن `Products` Collection. هذا يعني أن سعر الوجبة المخزن في قاعدة البيانات سيكون بتلك العملة.
2.  **تحديد سعر الصرف من قبل المدير:** يقوم المدير بتحديث سعر الصرف (مثلاً: 1 دولار = 15000 ليرة سورية) عبر واجهة الإدارة. يتم تخزين هذا السعر في `CurrencyRates` Collection.
3.  **عرض الأسعار للعميل:**
    *   عند تصفح العميل لقائمة الوجبات، سيتم عرض السعر بالعملة الأساسية للمطعم.
    *   بالإضافة إلى ذلك، سيتم عرض ما يعادله بالعملة الأخرى. على سبيل المثال، إذا كان سعر الوجبة 10 دولارات، وسعر الصرف 15000 ليرة سورية للدولار، فسيظهر للعميل: 
    *   **10 USD (150,000 SYP)** إذا كانت العملة الأساسية دولار.
    *   **150,000 SYP (10 USD)** إذا كانت العملة الأساسية ليرة سورية.
    *   تتم عملية التحويل هذه في الواجهة الخلفية (Node.js) عند جلب بيانات المنتجات، أو يمكن إجراؤها في الواجهة الأمامية (Flutter) بعد جلب سعر الصرف.
4.  **الدفع:** عند إتمام الطلب، يتم تسجيل المبلغ الإجمالي بالعملة التي اختارها العميل للدفع (أو العملة الأساسية للمطعم إذا لم يكن هناك خيار للعميل) في `Orders` Collection.

## 2. آلية التتبع المتقدمة ورسم المسارات (Advanced Tracking & Route Drawing)

تعتمد هذه الآلية على الاستفادة من WebSockets وخدمات الخرائط لتوفير تجربة تتبع لحظية ودقيقة لكل من العميل والسائق.

### أ. تتبع العميل للطلب (Customer Tracking)

1.  **إرسال إحداثيات السائق:** يقوم تطبيق السائق بإرسال إحداثياته الجغرافية (خط الطول وخط العرض) إلى الخادم (Node.js) عبر اتصال WebSocket كل 5-10 ثوانٍ عندما يكون السائق في حالة توصيل نشطة. يتم تخزين هذه الإحداثيات مؤقتاً في Redis لسرعة الوصول إليها، ويمكن أيضاً تسجيلها في `driverLocationHistory` ضمن `Orders` Collection لأغراض التتبع التاريخي.
2.  **بث الإحداثيات للعميل:** يقوم الخادم ببث هذه الإحداثيات إلى العميل المتصل بنفس غرفة الطلب (Socket.io Room) عبر WebSocket.
3.  **عرض التتبع في واجهة العميل:** يستقبل تطبيق العميل هذه الإحداثيات ويقوم بتحديث موقع أيقونة السائق على الخريطة بشكل لحظي. يمكن استخدام مكتبات Flutter للخرائط (مثل `google_maps_flutter` أو `mapbox_gl`) لرسم خط المسار المتوقع من موقع السائق الحالي إلى موقع العميل، وتحديث هذا المسار ديناميكياً مع حركة السائق.

### ب. تتبع موظف التوصيل لمكان العميل ورسم المسار (Driver Tracking & Route Drawing)

1.  **الحصول على موقع العميل:** عند قبول السائق للطلب، يقوم تطبيق السائق بجلب عنوان التوصيل النهائي للعميل من `Orders` Collection (والذي يتضمن إحداثيات `location: { type: 'Point', coordinates: [long, lat] }`).
2.  **رسم المسار:** باستخدام Google Maps API (أو Mapbox API)، يقوم تطبيق السائق بطلب رسم مسار من موقعه الحالي إلى موقع العميل. هذه الخدمات توفر واجهات برمجة تطبيقات لرسم المسارات على الخريطة، مع إمكانية عرض معلومات مثل المسافة والوقت المتوقع للوصول.
3.  **التوجيه الصوتي والمرئي:** يمكن دمج خدمات التوجيه الصوتي والمرئي لمساعدة السائق على الوصول إلى وجهته بكفاءة.

### ج. الفهرسة الجغرافية (Geospatial Indexing) في MongoDB

لتحقيق كفاءة عالية في عمليات البحث عن المواقع (مثل البحث عن أقرب سائق أو مطعم)، يجب استخدام الفهرسة الجغرافية:
*   **`Users` Collection (للسائقين):** يجب أن يحتوي حقل `driverInfo.currentLocation` على فهرس `2dsphere`.
*   **`Restaurants` Collection:** يجب أن يحتوي حقل `address.location` على فهرس `2dsphere`.
*   **`Orders` Collection:** يجب أن يحتوي حقل `deliveryAddress.location` على فهرس `2dsphere`.

هذه الفهارس تسمح لـ MongoDB بإجراء استعلامات جغرافية معقدة بسرعة، مثل `find nearest` أو `find within a radius`.
