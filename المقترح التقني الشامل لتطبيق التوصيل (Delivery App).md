# المقترح التقني الشامل لتطبيق التوصيل (Delivery App)

بناءً على متطلباتك المتمثلة في استخدام إطار عمل **Flutter** للواجهات وبيئة **Node.js مع تقنية WebSockets** للخادم، وقاعدة بيانات **MongoDB**، وتوفير أربع واجهات مختلفة (العميل، السائق، المطعم، الإدارة)، يقدم هذا المستند مقترحاً شاملاً لأفضل آلية عمل. تهدف هذه الآلية إلى ضمان أداء عالٍ، وتجربة مستخدم ممتازة، وقابلية للتوسع في المستقبل لتلبية احتياجات السوق المتزايدة، مع تضمين ميزات متقدمة مثل تحويل العملات الديناميكي والتتبع اللحظي.

## 1. البنية التقنية المقترحة (Tech Stack)

للحصول على أفضل نتيجة في تطبيقات التوصيل، لا يُنصح بالاعتماد الكلي على تقنية WebSockets في جميع العمليات. الآلية الأفضل والأكثر كفاءة هي الدمج بين **REST API** للعمليات العادية والبيانات الثابتة، و **WebSockets** للعمليات اللحظية التي تتطلب تحديثات مستمرة. يوضح الجدول التالي البنية التقنية المقترحة لكل مكون من مكونات النظام:

| المكون التقني | التقنية المقترحة | المبررات والاستخدامات |
| :--- | :--- | :--- |
| **الواجهات (Frontend)** | Flutter | يوفر إمكانية بناء تطبيقات لأنظمة Android و iOS و Web من قاعدة برمجية واحدة، مما يقلل وقت وتكلفة التطوير. |
| **الخادم (Backend)** | Node.js + Express.js + Socket.io | بيئة ممتازة للتعامل مع الاتصالات المتزامنة. يُستخدم Express.js لبناء REST API، و Socket.io لإدارة اتصالات WebSockets اللحظية. |
| **قاعدة البيانات الرئيسية** | MongoDB | قاعدة بيانات NoSQL مرنة وقابلة للتوسع، ممتازة للتعامل مع البيانات الجغرافية (Geospatial Data) والمستندات المتغيرة. |
| **قاعدة البيانات اللحظية** | Redis | قاعدة بيانات تعمل في الذاكرة (In-Memory)، ضرورية جداً لتخزين مواقع السائقين اللحظية، وإدارة جلسات WebSockets، والتخزين المؤقت (Caching) لتسريع الاستجابة. |
| **تخزين الملفات** | AWS S3 أو Cloudinary | خدمات تخزين سحابية مخصصة للتعامل مع ملفات الوسائط مثل صور الوجبات، وشعارات المطاعم، وصور الملفات الشخصية للمستخدمين. |
| **الخرائط والمواقع** | Google Maps API أو Mapbox | خدمات أساسية لحساب المسافات بدقة، وتوفير مسارات التوجيه (Routing)، وعرض التتبع اللحظي للسائقين على الخريطة. |

## 2. هيكلية قاعدة البيانات (MongoDB Schema)

تم تصميم هيكلية البيانات لتكون مرنة وتدعم المتطلبات المتقدمة للمشروع. فيما يلي تفاصيل المجموعات (Collections) الرئيسية:

### أ. مجموعة المستخدمين (Users Collection)
تحتوي على جميع أنواع المستخدمين مع حقل `role` لتحديد الصلاحيات.

| الحقل | النوع | الوصف | ملاحظات |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | المعرف الفريد للمستخدم | يتم إنشاؤه تلقائياً |
| `name` | String | الاسم الكامل للمستخدم | مطلوب |
| `email` | String | البريد الإلكتروني للمستخدم | مطلوب، فريد |
| `password` | String | كلمة المرور المشفرة | مطلوب |
| `phone` | String | رقم الهاتف | مطلوب، فريد |
| `role` | String | دور المستخدم | `customer`, `driver`, `restaurant`, `admin` |
| `status` | String | حالة الحساب | `active`, `inactive`, `blocked` |
| `address` | Object | عنوان العميل | `location: { type: 'Point', coordinates: [long, lat] }` |
| `restaurantId` | ObjectId | معرف المطعم المرتبط | للمطاعم فقط |
| `driverInfo` | Object | معلومات السائق | `availability`, `currentLocation: { type: 'Point', coordinates: [long, lat] }` |

### ب. مجموعة المطاعم (Restaurants Collection)
تحتوي على معلومات المطاعم التي تقدم الوجبات.

| الحقل | النوع | الوصف | ملاحظات |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | المعرف الفريد للمطعم | |
| `name` | String | اسم المطعم | مطلوب |
| `logo` | String | رابط شعار المطعم | مطلوب |
| `address` | Object | عنوان المطعم | `location: { type: 'Point', coordinates: [long, lat] }` |
| `ownerId` | ObjectId | معرف مالك المطعم | مرجع إلى `Users` |
| `status` | String | حالة المطعم | `open`, `closed`, `busy` |
| `menu` | Array of ObjectId | قائمة الوجبات | مرجع إلى `Products` |

### ج. مجموعة المنتجات/الوجبات (Products Collection)
تحتوي على تفاصيل الوجبات التي يقدمها كل مطعم.

| الحقل | النوع | الوصف | ملاحظات |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | المعرف الفريد للمنتج | |
| `restaurantId` | ObjectId | معرف المطعم | مرجع إلى `Restaurants` |
| `name` | String | اسم الوجبة | مطلوب |
| `image` | String | رابط صورة الوجبة | مطلوب |
| `price` | Number | سعر الوجبة | مطلوب |
| `currency` | String | العملة الأساسية للسعر | `SYP` أو `USD` |
| `isAvailable` | Boolean | هل الوجبة متاحة حالياً | `true`, `false` |

### د. مجموعة الطلبات (Orders Collection)
تحتوي على تفاصيل كل طلب يتم إنشاؤه.

| الحقل | النوع | الوصف | ملاحظات |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | المعرف الفريد للطلب | |
| `customerId` | ObjectId | معرف العميل | مرجع إلى `Users` |
| `restaurantId` | ObjectId | معرف المطعم | مرجع إلى `Restaurants` |
| `driverId` | ObjectId | معرف السائق | مرجع إلى `Users` |
| `items` | Array of Objects | قائمة الوجبات المطلوبة | `productId`, `quantity`, `price` |
| `totalAmount` | Number | إجمالي مبلغ الطلب | مطلوب |
| `currency` | String | العملة التي تم بها الطلب | `SYP` أو `USD` |
| `status` | String | حالة الطلب | `pending`, `accepted`, `preparing`, `ready`, `onTheWay`, `delivered` |
| `deliveryAddress` | Object | عنوان التوصيل | `location: { type: 'Point', coordinates: [long, lat] }` |
| `driverLocationHistory` | Array of Objects | سجل مواقع السائق | للتتبع التاريخي |

### هـ. مجموعة أسعار صرف العملات (CurrencyRates Collection)
تخزن أسعار الصرف التي يحددها المدير.

| الحقل | النوع | الوصف | ملاحظات |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | المعرف الفريد لسعر الصرف | |
| `baseCurrency` | String | العملة الأساسية | `USD` |
| `targetCurrency` | String | العملة المستهدفة | `SYP` |
| `rate` | Number | سعر الصرف | كم ليرة سورية مقابل 1 دولار |
| `updatedBy` | ObjectId | معرف المدير | مرجع إلى `Users` |

*ملاحظة هامة:* يجب استخدام الفهرسة الجغرافية (Geospatial Indexing) من نوع `2dsphere` على جميع حقول `location` لضمان سرعة البحث عن أقرب سائق أو مطعم.

## 3. نظام تحويل العملات الديناميكي

يهدف هذا النظام إلى توفير مرونة في عرض الأسعار والمدفوعات بين الليرة السورية (SYP) والدولار الأمريكي (USD).

1.  **تحديد العملة الأساسية:** يحدد كل مطعم العملة الأساسية لأسعاره (SYP أو USD) في حقل `currency` ضمن `Products`.
2.  **تحديث سعر الصرف:** يقوم المدير بتحديث سعر الصرف (مثلاً: 1 دولار = 15000 ليرة سورية) عبر واجهة الإدارة، ويُحفظ في `CurrencyRates`.
3.  **عرض الأسعار المزدوج:** عند تصفح العميل للقائمة، يقوم الخادم (أو التطبيق) بحساب السعر المعادل وعرضه. على سبيل المثال، وجبة بسعر 10 دولارات ستظهر كالتالي: **10 USD (150,000 SYP)**.
4.  **الدفع:** يتم تسجيل المبلغ الإجمالي بالعملة التي اختارها العميل للدفع في `Orders`.

## 4. آلية التتبع المتقدمة ورسم المسارات

تعتمد هذه الآلية على WebSockets وخدمات الخرائط لتوفير تجربة تتبع لحظية.

### أ. تتبع العميل للطلب
1.  يقوم تطبيق السائق بإرسال إحداثياته (GPS) إلى الخادم عبر WebSocket كل 5-10 ثوانٍ.
2.  يقوم الخادم ببث هذه الإحداثيات إلى العميل المتصل بنفس غرفة الطلب (Socket.io Room).
3.  يستقبل تطبيق العميل الإحداثيات ويحدث موقع أيقونة السائق على الخريطة، مع رسم خط المسار المتوقع ديناميكياً.

### ب. تتبع السائق لمكان العميل ورسم المسار
1.  عند قبول الطلب، يجلب تطبيق السائق إحداثيات موقع العميل من `Orders`.
2.  باستخدام Google Maps API (أو Mapbox API)، يطلب تطبيق السائق رسم مسار من موقعه الحالي إلى موقع العميل.
3.  يتم عرض المسار على الخريطة مع توفير التوجيه (Navigation) والوقت المتوقع للوصول.

## 5. دورة حياة الطلب (Order Lifecycle)

1.  **الطلب:** يختار العميل الوجبات ويتم الدفع (عبر REST API).
2.  **الإشعار:** يُنشئ الخادم "غرفة" (Room) في Socket.io خاصة بالطلب، وينضم إليها العميل.
3.  **القبول:** يصل إشعار لحظي للمطعم، يقبل الطلب ويبدأ التحضير (ينضم للغرفة).
4.  **البحث عن سائق:** يبحث الخادم في MongoDB (باستخدام الاستعلامات الجغرافية) عن أقرب السائقين المتاحين ويرسل لهم إشعاراً عبر WebSockets.
5.  **التوصيل:** يقبل سائق الطلب (ينضم للغرفة). يبدأ الخادم باستقبال إحداثيات السائق وبثها للعميل.
6.  **الانتهاء:** يُسلم الطلب، تتغير الحالة إلى "مكتمل"، ويُغلق اتصال WebSocket.

## 6. تفاصيل الواجهات الأربع

1.  **واجهة العميل (Customer App):** تركز على تجربة مستخدم سلسة، تصفح سريع، عرض أسعار مزدوج، وتتبع حي للسائق على الخريطة.
2.  **واجهة السائق (Driver App):** تركز على استهلاك منخفض للبطارية، استقبال الطلبات القريبة، وخريطة توجيه (Navigation) للوصول للعميل.
3.  **واجهة المطعم (Tablet App):** تركز على وضوح التنبيهات الصوتية والمرئية للطلبات الجديدة، وسهولة إدارة القائمة وحالة الطلبات.
4.  **واجهة الإدارة (Admin Panel):** لوحة تحكم ويب (Flutter Web أو React) لمراقبة الخريطة الحية، إدارة المستخدمين، وتحديث أسعار صرف العملات.

## 7. خارطة طريق المشروع (Project Roadmap)

لضمان التنفيذ الفعال، يُقترح تقسيم المشروع إلى المراحل التالية (المدة الإجمالية المقدرة: 4.5 - 6.5 أشهر):

| المرحلة | المهام الرئيسية | المدة المقدرة |
| :--- | :--- | :--- |
| **1. التخطيط والتصميم** | تحليل المتطلبات، تصميم MongoDB Schema، تصميم UI/UX، إعداد بيئة التطوير. | 2-3 أسابيع |
| **2. تطوير الواجهة الخلفية (Node.js)** | إعداد الخادم، المصادقة، تطوير REST APIs، إعداد Socket.io، نظام العملات، تكامل الخرائط. | 6-8 أسابيع |
| **3. تطوير الواجهات (Flutter)** | بناء تطبيقات العميل، السائق، المطعم، ولوحة الإدارة، تكامل APIs و WebSockets، إدارة الحالة. | 8-12 أسبوعاً |
| **4. الاختبار والنشر** | اختبار النظام المتكامل (E2E)، اختبار الأداء والأمان، إعداد CI/CD، النشر على المتاجر. | 3-4 أسابيع |

باتباع هذه الآلية المعمارية والممارسات التقنية، ستتمكن من بناء تطبيق توصيل احترافي، يتميز بأداء عالٍ، وقابلية للتوسع المستقبلي، ويقدم تجربة مستخدم تضاهي أفضل التطبيقات العالمية في هذا المجال.
